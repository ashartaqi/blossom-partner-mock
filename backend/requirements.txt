Django>=5.2
djangorestframework
djangorestframework-simplejwt
django-cors-headers
python-dotenv
requests

# Serving it. gunicorn runs the app; whitenoise serves the static files and the
# built SPA, which Django itself will not do once DEBUG is off.
gunicorn
whitenoise

# The OpenID Provider. Authlib owns every protocol decision in oidc/ — request
# parsing, redirect_uri validation, PKCE comparison, ID token signing, spec-shaped
# error responses. None of that is hand-rolled here, which is the point.
authlib>=1.3,<2
cryptography

# Imported directly by oidc/keys.py and the tests. It arrives as an Authlib
# dependency today, but a library we import ourselves belongs here rather than
# resting on someone else's dependency tree.
joserfc
